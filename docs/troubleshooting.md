# Troubleshooting

> Status: placeholder — will be populated as each subsystem ships. Planned topics: browser fails to start, noVNC fails to connect, scanner unavailable, Session Agent unavailable, DNS/network issues, quarantine storage issues, MFA problems, database issues, Redis issues.
